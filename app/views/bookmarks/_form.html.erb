<%= form_with(model: bookmark, remote: true) do |form| %>
  <% if bookmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookmark.errors.count, "error") %> prohibited this bookmark from being saved:</h2>

      <ul>
      <% bookmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "URL" %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.select :category_id, @category_list, prompt: "Seleccione una opción" %>
  </div>

  <div class="field">
    <%= form.label "Type" %>
    <%= form.select :kind_id, @kind_list, prompt: "Seleccione una opción" %>
  </div>


  <div class="actions">
    <%= form.submit "Save", data: { confirm: 'Are you sure?' } %>
  </div>
<% end %>
